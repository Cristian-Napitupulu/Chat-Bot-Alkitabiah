# Description: This file is used to setup the project and run the necessary scripts to train the models and create the charts.

import data.olah_intents
import data.create_chart

import train_bert
import train_neural_network